# ChainReaction - progress

## Current step
Step 2 complete, plus two out-of-sequence fixes (borders/contrast, job-seeker demo walkthrough - see Done below). Repo: https://github.com/Radha1008/chainreaction-ewgh-2026 - Live: https://radha1008.github.io/chainreaction-ewgh-2026/ Next session does Step 3 (women's safety layer, data only).

## Done
- **Cipher lands on results + no em dashes** (20 Sep 2026): Cipher's "Run assessment now" now calls `navigate("full")`, runs the simulation, then scrolls/focuses the results heading (jump under reduced motion) and collapses its panel to a compact header instead of covering the results; final message is "Done. You're on the results now." Every em dash (raw, `&mdash;`, `—`) across index.html/app.js/data.js/styles.css was hand-replaced with a comma, period, colon, or parentheses, chosen per context, none were left as spaced hyphens; verified with `tmp/audit-emdash.js` (gitignored CDP script) grepping source plus rendering Home/Quick check/Full assessment/demo results/walkthrough card in both themes - 0 hits.
- **Carousel/modal overflow + Cipher button fix** (20 Sep 2026): fixed `.library-track .library-card`'s stray `margin: 2px` (was making the active slide 4px wider than its `.carousel-viewport`, clipping its right edge and forcing a horizontal scrollbar via `.modal-body`'s auto-overflow-x quirk); added `overflow-x: hidden` to `.modal-body`, `min-width: 0` to `.carousel-viewport`/`.carousel-track`/`.step-card`/library cards, capped `.carousel-wide` at `max-width: 100%`, and moved `.carousel-arrow-prev/-next` from off-panel negative offsets to small positive insets so they render fully inside the modal in both `howModal` and `libraryModal`. Also: Cipher's "Run assessment now" button now flips from "Running..." to "Done" once `runSimulation`'s 1350ms delay completes. Verified with `tmp/audit-carousel.js` (gitignored CDP script) at 390/768/1280px in both themes - 0 failures.
- **Demo guided walkthrough** (20 Sep 2026): `renderChainFlow` now detects a `tourSteps`-bearing path (authored only for `fake_recruiter_chain`, data.js) and, after a one-time calm draw (700ms/node vs the normal 340ms; instant under `prefers-reduced-motion`), calls `startTour()` to land on step 1 and **wait** - it never auto-advances, and Attacker View / Simulate MFA never auto-run. New app.js block "guided walkthrough" (`startTour`/`endTour`/`tourNext`/`tourBack`/`renderTourStep`/`runTourFix`/`concludeTour`) reuses the existing `#nodeDetail` panel as the docked walkthrough card (counter, Back/Next, Skip tour, four-part body: What happens / Why it's connected / What an attacker sees / What breaks it) rather than a new floating popover - the spec's "docked card" alternative, all screen sizes. All 7 steps' four-part text is authored data (`fake_recruiter_chain.tourSteps`, data.js); the last step carries `isFixStep` and embeds "Simulate MFA", which (after a 1.5s dashed/faded preview with a `-N paths` badge) marks the downstream nodes broken and swaps the card to "The chain broke." + "Replay the tour". Hovering/focusing any flow-node (any chain, not just the tour one) shows a floating `.node-tooltip` ("click for the full breakdown"); touch gets the same via `touchstart`. Keyboard: Left/Right arrows step through, Esc ends the tour; Tab+Enter works natively since every control is a real button. Clicking a node always reopens the tour at that step for tour-eligible chains (was the legacy 3-part `showNodeDetail` panel, which non-tour chains still use unchanged). Attacker-view toggle re-renders the open tour card and adds `.tour-emphasis` to the "what an attacker sees" section. Fixed a bug found while wiring this up: `buildPaths` (app.js) rebuilds a path object from named chosen fields and was silently dropping `tourSteps` (and would drop any future per-chain field the same way) - added it explicitly. Verified with three headless-CDP scripts (no puppeteer, not committed - regenerate from this description if needed): (1) the brief's exact spec - `?demo=1` stays at step 1 after 3s idle, Next through all 7 steps with counter/title assertions, node hover shows tooltip, Simulate MFA -> "The chain broke."; (2) Back/Skip/arrow-keys/Esc/node-click-reopens-tour; (3) hover-preview badge + dashed nodes before click, and Replay the tour resets state. Also re-ran the Step 1 border audit (below) after the CSS additions - still 0 failures.
- **Borders & light-theme contrast** (20 Sep 2026): Paper's `--border` was only ~1.4:1 against its own card/bg tokens (WCAG non-text contrast wants 3:1), so cards, chips, stat tiles and the exposure-map nodes read as loose text with no edge. Bumped `--border` in both themes to real 3:1+ values and added a new `--border-strong` token (used on `.stat`, `.metric`, `.figure`, and available for future emphasis) - both themes verified against every relevant `--bg*` token, not just one. Gave `.habit-card` (was `border: 1px solid transparent`) and the home hero `.figure` stats (had no border/background at all) an actual card treatment. Replaced `border-color: var(--accent-glow)` on 6 hover/focus states (persona card, legend item, carousel arrow, library card, glossary card, cipher input) with solid `var(--accent)` - the translucent glow blended down under 2:1 against Paper backgrounds, i.e. the border nearly vanished on hover. Swapped 3 stray hardcoded `rgba(...)` borders (leftover from before the Step 1 token rebuild) for `var(--accent)`/`var(--success)`. Surface-map nodes (`.surface-node`, `.surface-center`) gained a `box-shadow: var(--shadow-soft)` so the per-stage ring colour always reads as a distinct surface even where a specific stage hue is borderline. Verified with a headless-CDP script (`tmp/audit-borders.js`, gitignored, not committed) that walks every card/tile/panel/chip/map selector in both themes and checks computed `border-color` contrast (or an accepted `box-shadow` fallback) >= 3:1 against its effective parent background: 0 failures (was 6 in Paper + 4 in Ink before the fix - Ink's `.scenario-card` and hero `.surface-node` were failing too, not just Paper, so both themes' `--border` got the same treatment).
- **Step 2** (20 Sep 2026): the job-seeker demo path, end to end. First fixed the two engine bugs it depended on (see Known bugs below): wired `setupWeakestLink` into `renderSelectedPath`, and replaced the by-index `habitsInPath` lookup in `attackerLineFor`/`showNodeDetail` with an authored `stepHabits` array per chain (data.js, all 15 named chains) plus deterministic `stepHabits` for generated paths (app.js `buildPaths`) - each step now attributes to the habit that actually introduced it, or `null` (generic fallback text) for a connecting/narrative step, never a wrong habit. `fake_recruiter_chain` gained a `no_email_2fa` requirement/step so its weakest link is genuinely MFA-related; `DEMO_PROFILE.habits` trimmed to exactly that chain's 4 required habits + `no_email_2fa` so the demo matches one unambiguous path. Added `?demo=1` URL support (bootstrap checks `URLSearchParams`) and relabelled the hero CTA to "Try the job-seeker scenario". `setupWeakestLink`'s fix button now reads "Simulate MFA" when the weakest link is an MFA habit, shows real before/after path counts, and opens on "The chain broke." Demo banner copy matches the brief's exact "Fictional example - no real data." Verified with one headless-Chrome script (CDP, no puppeteer) walking: demo loads via `?demo=1` -> chain renders (7 nodes, correct scenario) -> fictional-data label visible -> Attacker View toggle changes flow-node text -> node click opens "Why this link exists" with correct habit attribution -> weakest-link panel populated -> "Simulate MFA" clicked -> "The chain broke." shown, downstream nodes visually broken. All 12 assertions pass. Two screenshots taken (ink + paper) confirming the chain flow renders legibly in both themes.
- **Step 1** (20 Sep 2026): design tokens rebuilt as two themes. **Ink** (default, dark) = deep aubergine-black `#151018`; **Paper** (light) = warm off-white `#f6f2ec` with aubergine ink. One signature accent (raspberry `#ef5f85` / `#b01e4d`) reserved for the attack path; one counter-colour (teal `#4dd4b0` / `#0f7a63`) for defences. Google Fonts CDN removed - system stacks only (new `--font-display` serif on `.hero-title`, `.home-h2`, `.page-title`, `.hero-emphasis`). Header theme toggle (`#themeToggle`) added: in-memory only, defaults to `prefers-color-scheme`, follows system changes until the reader chooses. Every hardcoded colour outside `@media print` moved to tokens (`--on-accent`, `--on-success`, `--paper-text`, `--paper-accent`, `--shadow-strong`/`--shadow-soft`, `--grid-line`, `--chrome-veil`, `--accent-deep`). Required footer disclaimer added (was missing entirely). Verified in headless Chrome on home / quick check / full assessment / demo results in BOTH themes, plus a scripted WCAG audit over every visible text node: 0 genuine failures (the only flagged items were `.btn-primary`, whose gradient fill the audit could not read; both stops checked by hand at 4.7:1 and 9.0:1).
- **Step 0** (20 Sep 2026): live Pages URL verified serving index.html + all three assets (HTTP 200). File-timestamp evidence exported to `../chainreaction-file-dates.csv` (both local copies captured; Desktop copy retains the 19 Sep creation times and is now the repo). Git initialised on `main` with `.gitignore`, first commit of all prior work, public GitHub repo created, pushed, Pages enabled. CODE MAP written below.

## Next
- **Step 3** - women's safety layer, data only (new scenarios in data.js: recruiter impersonation is already covered by Step 2's chain; add shared accounts/location history with an ex-partner, harassment/doxxing, solo commute/travel, dating-app meetup). Homepage audience copy, Quick exit button, support note. No new engine code - if a new named chain needs per-step habit attribution, add its `stepHabits` array the same way the Step 2 fix did.

## Known bugs
All four Step 1 bugs above are fixed. Zero `http(s)://` references remain in any of the four files.

Fixed in Step 2 (were blocking it):
- `setupWeakestLink` is now called from `renderSelectedPath`, so the weakest-link/"Simulate MFA" panel renders.
- `attackerLineFor`/`showNodeDetail` now read a per-chain `stepHabits` array instead of indexing `habitsInPath` by step position.

Engine bugs found by a code review of the initial commit (still NOT fixed - none are Step 1 or 2 work; fold the relevant ones into Steps 4/6):
- `simulateIntervention` (app.js, generic interventions panel) rebuilds from the live `selected` set but compares against the run-time snapshot, so before/after counts can claim a fix *adds* paths.
- `renderSelectedPath` doesn't re-apply `currentPerspective`, so switching paths silently reverts the text to My view while the Attacker view button stays highlighted.
- Three chains in data.js (`qr_code_trap`, `public_profile_puzzle`, `overshared_workspace`) set `weakestLinkStepIndex` to the final step index, so those chains render with no escalation stage. (`fake_recruiter_chain` was a fourth instance of this; fixed incidentally by the Step 2 MFA-step addition.)
- Quick check uses `assembleGenericChain`, Full assessment uses `buildPaths` - the two surfaces give different answers for identical input.
- `runSimulation` is re-entrant (run button never disabled during the 1350 ms delay); `resetResults` doesn't clear `currentPaths`/`currentPathIndex`/`simulationState`.

Cosmetic, deferred to Step 6:
- Paper theme: stage icon rings (`--stage-initial_access` amber especially) read pale against the off-white. Icons carry the meaning so it isn't colour-only. Mitigated today (border audit fix): `.surface-node`/`.surface-center` now also carry a `box-shadow: var(--shadow-soft)` so the surface always reads as distinct even where a specific stage hue is borderline, but the ring colour itself still isn't individually contrast-checked per stage - do that properly in Step 6.
- Disabled `.btn-primary` ("See my result" at 0 of 6) sits at `opacity: 0.35`; legible but weak. Disabled controls are WCAG-exempt.

## Deferred
- Step 7 (rule-based "Ask the simulator" chips) - only if Steps 1-6 are tested before 3:30 PM. Note: a rule-based assistant named **Cipher** already exists (`setupCipher`, `cipherRespond`, FAQ/GLOSSARY/KEYWORD_MAP in data.js); Step 7 may reduce to relabelling and adding question chips rather than new code.
- PNG export of the Group Chat Card (Step 4) - text copy + print layout first.
- Demo walkthrough card is docked below `#chainFlow` (reuses `#nodeDetail`), not a floating popover anchored beside the active node - the brief allowed either; docked was simpler and already passes the border/contrast audit. A true anchored popover (positioned via the node's `getBoundingClientRect`, flipping side near viewport edges) is a nice-to-have if it's ever requested specifically.
- "Tap acts as hover" for the node tooltip is approximate: touch fires the tooltip on `touchstart` and then the normal `click` still opens the full card right after, rather than a strict two-tap preview-then-open pattern. Nobody has flagged this on a real device yet; revisit if they do.
- Guided-walkthrough four-part text (`tourSteps`) is authored only for `fake_recruiter_chain`. Other named chains still use the older 3-part `showNodeDetail` panel on node click. If a future chain needs the same tour treatment, add its `tourSteps` array the same way (data.js) and it picks up the tour automatically (`renderChainFlow` gates on `path.tourSteps` being present and the same length as `path.steps`).

## CODE MAP
Four files, repo root, no build step. Load order: `data.js` then `app.js` (both plain `<script>` at end of `index.html`).

**index.html** (556 lines) - single page, three `<section class="page">` panels swapped by JS: `#pageHome`, `#pageQuick`, `#pageFull`. Header `.topbar` has `.brand` + `.sitenav` buttons carrying `data-page`. `#pageFull` holds three `.view` panels: `#viewProfile` (situation/persona/habit pickers, `#surfaceMap`, run bar), `#panelChain` (`#resultsEmpty`, `#analyzing`, `#resultsChain` with `#pathList`/`#chainFlow`/`#nodeDetail`/`#severityCard`/`#interventionList`/`#weakestLinkPanel`, `#resultsNoChain`), `#panelThreat` (`#threatBars`). Below: `.site-footer`, two modals (`#howModal`, `#libraryModal`, both carousel-based), and the Cipher chat widget (`#cipherPanel`, `#cipherForm`, `#cipherToggle`). Nearly all content is rendered by JS into these IDs. Lines 7-9 = the Google Fonts CDN links to remove.

**data.js** (~810 lines) - all content, no logic. `HABITS` (line 14, ~30 entries; each has `id, label, shortName, stage, role` (entry/weakness/signal), `primaryThreat, entryStep, entryConsequence, attackerView, fixAdvice`). `SEVERITY_WEIGHTS` (307), `GENERIC_FINAL_IMPACT` (323), `STAGE_META` (326), `THREAT_ARCHETYPES` (334), `QUICK_CHECK` (364, six questions), `SITUATIONS` (376), `DEMO_PROFILE` (391), `PERSONAS` (401), `KEYWORD_MAP` (434), `FAQ` (466), `GLOSSARY` (491), `CHAINS` (512, hand-authored named scenarios: `id, name, objective, requiredHabits, weakestLink, weakestLinkStepIndex, stepHabits, realWorld, steps[]`; `stepHabits[i]` is the habit id that step narrates, or `null` for a connecting/narrative step - added in Step 2, keep authoring it for any new chain). `fake_recruiter_chain` additionally carries `tourSteps[]` (one entry per `steps[]`, each `{title, whatHappens, whyConnected, whatAttackerSees, whatBreaksIt}`; the last has `isFixStep: true`) - the guided-walkthrough content, added today. **New scenarios for Step 3 go in `CHAINS` + `HABITS` here, no engine changes; give them `tourSteps` only if they should get the same guided walkthrough as the demo.**

**app.js** (~2130 lines, line numbers shift often - re-grep before trusting any of these) - one IIFE, in-memory `selected` Set, nothing persisted. Rules engine: `findNamedChains` (345, hand-authored chains win) -> `assembleGenericChain` (352, generic assembler) -> `buildPaths` (395, also builds `stepHabits` and copies `tourSteps` per path - added today, this is the field that silently got dropped once already, watch for the same thing with any future chain-only field) -> `computeInterventions` (~465) -> `runSimulation` (~505) / `executeChain` (~550). Scoring: `computeSeverity`, `renderSeverityCard`. Rendering: `renderSurfaceMap`/`updateSurfaceMap`, `renderHabitGrid`, `renderFindings`, `renderPathList`, `renderSelectedPath` (also calls `setupWeakestLink`), `renderChainFlow` (~1197, now stagger-paced - 700ms/node when `path.tourSteps` is present, else the original 340ms; also wires `attachNodeTooltip` per node and kicks off `startTour` after the initial draw for tour-eligible paths), `renderInterventions` + `simulateIntervention`, `setupWeakestLink` (reads path.weakestLink/weakestLinkStepIndex, labels its button "Simulate MFA" for MFA habits, now also calls `attachMfaHoverPreview`), `renderThreatModel`, `renderHomeScenarios`, `renderHeroMap`. Perspective (my/attacker) toggle: `attackerLineFor`, `applyPerspective` (now also re-renders an open tour card), `setupPerspectiveToggle`. Node detail: `showNodeDetail` (reads `path.stepHabits[index]`; still used for chains without `tourSteps`). **Guided walkthrough** (new block, just after `hideNodeDetail`, ~line 792): `tourState` (module-level, `null` when no tour is open), `startTour`/`endTour`/`tourNext`/`tourBack`/`tourKeydownHandler`/`renderTourStep` (writes into `#nodeDetail` with class `tour-mode` - reused rather than a new element), `runTourFix`/`concludeTour` (the "Simulate MFA" embedded in the last tour step), `computeMfaRemovedCount`/`setMfaPreview`/`attachMfaHoverPreview` (shared by both the tour's fix button and the page's real `#fixBtn`). `attachNodeTooltip` and `prefersReducedMotion` sit right before/after `renderChainFlow`. Routing: `navigate`/`setupRouting` + `activateTab`/`setupTabs`. Quick check: `renderQuickCheck`, `runQuickCheck`. Demo: `loadDemo`/`setupDemo`. URL `?demo=1` handled at the very bottom of the bootstrap IIFE. Motion: `setupReveals`, `countUp`. Shared widgets: `setupCarousel`, `setupModals`, Cipher `cipherRespond`/`setupCipher`.

**styles.css** (~4200 lines, line numbers shift often - re-grep) - **all theming is done (Step 1), border/contrast pass done today.** Lines 1-155ish hold the whole token system: base Ink tokens on `:root`, then the same Paper set twice - once in `@media (prefers-color-scheme: light) { :root:not([data-theme="ink"]) }` and once in `:root[data-theme="paper"]`, so an explicit toggle choice always wins. Token names: `--bg`/`--bg-elevated`/`--bg-card`/`--bg-card-hover`, `--border`/`--border-soft`/`--border-strong` (new today - a higher-contrast variant used on `.stat`/`.metric`/`.figure` and available for any future emphasis border), `--text`/`--text-dim`/`--text-faint`, `--accent`/`--accent-2`/`--accent-deep`/`--accent-soft`/`--accent-glow`/`--on-accent`, `--sage`/`--success`/`--success-soft`/`--on-success`, `--gold`, `--danger`, the counter-band set `--paper`/`--paper-text`/`--paper-dim`/`--paper-faint`/`--paper-border`/`--paper-accent`, `--shadow-strong`/`--shadow-soft`, `--grid-line`/`--grid-fade`, `--chrome-veil` (translucent sticky chrome), and `--stage-*` per kill-chain stage. Fonts: `--font-display` (serif), `--font-sans`, `--font-mono`, all system stacks. **Any new colour must be a token defined in all three blocks; `--border`/`--border-strong` must stay >=3:1 against every `--bg*` token in both themes - use `tmp/audit-borders.js` (gitignored CDP script, regenerate if missing) to check.** `.theme-toggle` + `.footer-disclaimer` rules are at the end of the file. Only `@media print` still carries hardcoded hexes, deliberately. New today, right after `.node-detail-body`: the "guided walkthrough" block (`.node-detail.tour-mode`, `.tour-*` classes, `.mfa-preview-badge`, `.flow-node.mfa-preview`, `.flow-node.tour-dim`/`.tour-active`, `.node-tooltip`) - also extended the existing `@media (prefers-reduced-motion: reduce)` block near the end to cover these. Later sections in file order (all shifted since Step 1's line numbers were recorded, don't trust the old numbers): shell/views, modals, print, nav, how-it-works, surface map, stage icons, result tabs, analyzing loader, chain flow, threat model, Cipher widget, site chrome, home, quick check, full-assessment subnav, editorial light section, scroll reveal, interaction polish, findings/metrics, path list + detail, node detail (+ guided walkthrough), interventions, sim result, situations + demo, focus rings + reduced motion. Breakpoints are scattered per-section (420/480/560/640/720/980/1080px).
